{
  "name": "sales-crm-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Sales CRM REST API — Express + Sequelize + MySQL",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node scripts/seed.js",
    "migrate": "node scripts/migrate.js",
    "recompute": "node scripts/recompute.js",
    "test": "node --test",
    "start:deploy": "node scripts/seed.js && node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.11.0",
    "sequelize": "^6.37.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
