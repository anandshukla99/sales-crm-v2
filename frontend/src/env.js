// This is a standalone, self-contained deployment: the app serves from the site root, with its own
// login form and self-service password change. There is no external portal or SSO, so the router
// base path is always the root.
export const BASE_PATH = '';
